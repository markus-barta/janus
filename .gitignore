# Rust workspace
/target
**/*.rs.bk
Cargo.lock.bak

# Go envelope keeps its own go-envelope/.gitignore (binary, coverage)

# Nix / devenv
/.devenv/
/.direnv/
/devenv.lock
/devenv.local.nix
/.tmp/

# Python smoke helpers
__pycache__/
*.py[cod]

# editor / OS
.DS_Store
*.swp
